Installing postgresql:

sudo apt-get install postgresql libpq-dev
pip3 install -r requirements.txt

Running postgresql:

sudo -i -u postgresql
createuser <name>
createdb <name>
exit
sudo adduser <name>

(Currently: gavel-test, GavelTest)

BE SURE TO DO THIS TO HAVE EVERYTHING READY:
python3 initialize.py

Getting the server to run:
Set the DB_URI = "postgresql://<user>:<password>@localhost/<db-name>"

export sslmode="require" #in bash before running.
