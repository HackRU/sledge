import chai from "chai";

const assert = chai.assert;

export function test() {

  // TODO: Actual tests
  describe("numbers", () => {
    it("adds", () => {
      assert(1+1 === 2);
    });
  });

}
